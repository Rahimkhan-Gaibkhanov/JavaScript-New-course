    "use strict";

    // №1

    // Проверьте, что будет результатом следующей операции:

    // alert(0.1 * 0.2);

    let num1 = 0.1 * 0.2;

    console.log(typeof num1.toFixed(2));
    console.log(num1.toFixed(2));
    console.log(typeof Number(num1.toFixed(2)));
    console.log(Number(num1.toFixed(2)));


    // №2

    // Проверьте, что будет результатом следующей операции:

    // alert(0.3 - 0.1);

    let num2 = 0.3 - 0.1;

    console.log(num2.toFixed(2))