    'use strict';

    // Задача 1

    // Скажите, что выведется в консоль в результате выполнения следующего кода:
    
    // let obj = {a: 1, b: 2, c: 3, d: 
    //     4, e: 5}; 
    // console.log('e' in obj);
    // console.log('f' in obj);

    // первый true так как в обекте есть 'e', второй false так как в обекте нет 'f'

    let obj = {a: 1, b: 2, c: 3, d: 
         4, e: 5}; 
     console.log('e' in obj);
     console.log('f' in obj);