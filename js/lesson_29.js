    'use strict';

    // Задача 1

    //Дан следующий код:

    let num1 = -1;
    let num2 = 50;
    
    if ( !(num1 >= 0 || num2 <= 10) ) {
        alert('верно');
    } else {
        alert('неверно');
    }

    // Используя оператор ! инвертируйте приведенное условие.