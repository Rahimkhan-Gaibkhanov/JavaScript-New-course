    "use strict";

    //Задача 1js.Pm.Bs.ID.1

    //  Переделайте этот код так, чтобы в нем операции инкремента и декремента:

    // let num = 10;
    // num = num + 1;
    // num = num + 1;
    // num = num - 1;
    // alert(num);

    let num = 10;
    num++;
    num++;
    num--;
    alert(num);

